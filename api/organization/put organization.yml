info:
  name: put organization
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:8080/organization
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
